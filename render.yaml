services:
  - type: web
    name: kazuchar-ai
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: RENDER
        value: true
      - key: JWT_SECRET
        generateValue: true
    healthCheckPath: /api/health
